import Foundation
import UIKit

struct Global {
    
    // toolbar config
    static var showToolbar = true
    static var toolbarFont = UIFont.systemFont(ofSize: 17, weight: UIFont.Weight.regular)
    static var toolbarTintColor = Color.black
    static var toolbarBackgroundColor = Color.white
}
